import React from "react";
import { ComposedChart } from "recharts";

const Chart = (props) => (
  <>
    {/* <ComposedChart
      layout="vertical"
      width={500}
      height={400}
      data={data}
      margin={{
        top: 20,
        right: 20,
        bottom: 20,
        left: 20,
      }}
    ></ComposedChart> */}
  </>
);

export default Chart;
